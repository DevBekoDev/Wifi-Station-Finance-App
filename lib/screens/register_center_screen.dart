import 'package:flutter/material.dart';

class RegisterCenterScreen extends StatelessWidget {
  const RegisterCenterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}